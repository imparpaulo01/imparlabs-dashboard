{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd query-app && npm install && npm run build",
  "outputDirectory": "query-app/dist",
  "framework": "vite",
  "installCommand": "echo 'Install handled in buildCommand'"
}
